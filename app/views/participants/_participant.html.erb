<li>
  <div class="group_block">
    <%= participant.name %>
    <div class="boom_button">
      <%= link_to 'x', participant, method: :delete, data: { confirm: 'Remover esse participante?' }, title: "Delete: #{participant.name}", class: [:btn, 'btn-xs', 'btn-danger'] %>
    </div>
  </div>
</li>