<%= form_for(@group) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <%= f.label :name, "Nome" %>
  <%= f.text_field :name %>
  
  <%= f.submit "Criar grupo", class: "btn btn-primary" %>
<% end %>