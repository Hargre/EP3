<%= form_for(@group) do |f| %>
  <%= f.label :nome %>
  <%= f.text_field :name %>

  <%= f.label :limit, 'Limite de preço' %>
  <%= f.text_field :limit %>

  <%= f.submit "Criar grupo", class: "btn btn-primary" %>
<% end %>