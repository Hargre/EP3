<% if logged_in? %>
  <div class="row">
      <section class="group_form">
        <%= render 'shared/group_form' %>
      </section>
  </div>
<% else %>
  <div class="center jumbotron">
    <h1>Amigo Secreto</h1>

    <h2>
      Bem-vindo ao gerador de combinações para amigo oculto!
    </h2>

    <p>
      <%= link_to "Cadastre-se agora!", cadastro_path, class: "btn btn-lg btn-primary"%>
      <%= link_to "Já cadastrado? Entre aqui!", entrar_path, class: "btn btn-lg btn-primary" %>
    </p>
  </div>
<% end %>
