<%= link_to "Sair", sair_path, method: "delete" %>
<%= link_to "Página inicial", root_url %>
<% if current_user.admin? %>
    <%= link_to "Usuários", indice_path %>
<% end %>

<div class="col-md-8">
  <% if @user.groups.any? %>
    <h1>Grupos (<%= @user.groups.count %>)</h1>
    <h4 class="groups">
      <%= render @groups %>
    </h4>
    <%= will_paginate @groups %>
  <% end %>
</div>
